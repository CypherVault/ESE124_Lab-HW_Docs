 #include <stdio.h>


/*

Implement the abstract data type (ADT) queue (FIFO) of strings. ADT has the following methods:
 clear – clears the queue;
 is_empty – returns 1 if the queue is empty, otherwise 0;
 is_full – returns 1 if the queue is full, otherwise 0; 
 add – adds a new string at the end of the queue; 
 remove – removes the string from the front of the queue.
 
  Use ADT queue to solve the following problems:
    • Write an interactive program that adds and removes elements from the queue. 
    • Use ADT queue to find the way out from a maze. The maze is an 8 x 8, 2 D array of characters, ‘*’ indicating a wall. 
	The program reads the starting point from which the way out is found.
	 The algorithm mimics the way concentric ripples are formed on the surface when throwing a stone into the water. 
	 This kind of search is called breadth-first-search.


*/

int main (void){
	
		
	char maze[8][8] = {'*', '*', '*', '*', '*', '*', '*', '*', 
                   '*', ' ', ' ', ' ', '*', '*', ' ', ' ', 
                   '*', ' ', '*', ' ', ' ', '*', ' ', '*', 
                   '*', '*', '*', ' ', ' ', '*', ' ', '*', 
                   '*', '*', '*', '*', ' ', ' ', ' ', '*', 
                   '*', '*', '*', '*', ' ', '*', '*', '*', 
                   '*', '*', '*', '*', '*', '*', '*', '*', 
                   '*', '*', '*', '*', '*', '*', '*', '*'};
	
	
	
	
}
